rules:
  alex:
    allow: []

  common-misspellings:
    ignore: []

  en-capitalization: true

  no-empty-section: true

  no-exclamation-question-mark: true

  one-sentence-per-line: true

  period-in-list-item: true

  rousseau:
    severity: warning
    showLevels:
    - suggestion
    - warning
    - error
    ignoreInlineNodeTypes:
    - Code
    - Html
    ignoreTypes:
    - passive            # covered by write-good
    - lexical-illusion
    - so
    - adverbs
    - weasel
    - sentence:uppercase # covered by en-capitalization

  spelling:
    language: en
    skipPatterns:
    - '/placeos/i'
    - '/backoffice/i'
    - '/\b\S*\.\S{2,4}/g'     # filenames
    - '/\b(?:[A-Z]){2,}\b/g'  # acronyms
    - '/[0-9]+/g'             # numbers 
    # capitalization isn't handled well here, 
    # might need the /i on any which are not in terminology?
    # for anything in terminology, add only the corrected version
    # Unrelated: do we add stanard shortenings like auth, docs, init, etc 
    - AOpen
    - balancer                
    - chatbot
    - Chromebase
    - ChromeBox
    - ChromeOS
    - CloudFormation
    - ElastiCache
    - Elasticsearch
    - etcd
    - Fargate
    - Frontends
    - Kubernetes
    - Mimo
    - Moki
    - NGINX
    - Optica
    - RethinkDB
    - SaaS
    - subnet
    - webhook
    

  terminology:
    defaultTerms: true
    terms:
    - ['back\W?office', 'Backoffice']
    - ['front[- ]end', frontend]
    - ['frontends', 'Frontends'] # the application, can conflict with "frontend" as a term?
    - ['place\W?os', 'PlaceOS']
    - ['place\W?tech(nology)?', 'Place Technology']
    - ['saas', SaaS]
    - "CloudFormation"
    - "ElastiCache"
    - "Elasticsearch"
    - "etcd"
    - "Kubernetes"
    - "NGINX"
    - "RethinkDB"

  write-good:
    severity: warning


filters:
  allowlist:
    allow:
    - '/<i>.*<\/i>/g'    # idiomatic terms
